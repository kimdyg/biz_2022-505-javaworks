package com.callor.app.service;

/*
 * 클래스의 한종류이지만
 * 모든method 의 구현체가 없는 상태
 * method의 이름만 정의된 상태
 * method의 prototype 만 정의된 상태
 */
public interface ScoreService {

	public void inputScore();	
	public void saveScore();
}
